// ex1.cs
using System;
using System.Windows;

// WPF 
// => Windows Presentable Framework
// => .net 기반(C#) GUI 라이브러리
// => WinForm 의 다음 버전(대체자)

// step1. 환경설정

class Program
{
    public static void Main()
    {
        MessageBox.Show("Hello, WPF");
    }
}