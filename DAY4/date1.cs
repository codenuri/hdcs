// date1.cs
using static System.Console;

// 프로그램에서 날짜를 다루는 경우가 많다.
// => Date 라는 타입을 만들자
class Date
{
    ? int year;
    ? int month;
    ? int day;
}


class Program
{
    public static void Main()
    {

    }
}
